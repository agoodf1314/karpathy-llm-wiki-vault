---
name: llm-wiki-lint
description: "知識庫全局健康度檢查。掃描 wiki/ 檢測死鏈、孤兒頁面、未同步索引和知識衝突。"
user-invocable: true
---

# Lint — 知識圖譜健康巡檢

## 路徑（Obsidian Vault）
- Vault: `/Users/wangwenhong/obsidian/`
- Wiki: `02-wiki/`

## 觸發
用戶要求「檢查知識庫狀態」、「健康檢查」時觸發。

## 流程

### Step 1：索引一致性檢查
1. 讀取 `02-wiki/index.md`
2. 掃描 `02-wiki/` 下所有 `.md`（排除 index.md 和 log.md）
3. 比對已註冊但檔案不存在的條目 OR 檔案存在但未註冊的頁面

### Step 2：雙向連結健康檢查
1. 掃描所有 `.md` 中的 `[[雙鏈]]` 格式連結
2. 連結目標不存在 → **死鏈**
3. 從未被任何頁面引用 → **孤兒頁面**

### Step 3：認知衝突審查
全局搜尋包含 `## 知識衝突` 的頁面。

### Step 4：過期頁面檢查
1. 檢查所有 `.md` 的 frontmatter 是否具備 `last_verified` 欄位
2. 60 天以上未驗證 → 列入「需 refresh」清單
3. 缺少 `confidence` 欄位 → 列入「需補欄位」清單

## 報告格式
```markdown
## 🩺 知識庫健康體檢報告 — YYYY-MM-DD

### ✅ 綠燈項
- [運行良好的項目]

### ⚠️ 黃燈項
- **發現 N 個孤兒頁面**：[列表]
- **發現 N 個未同步索引**：[列表]

### ❌ 紅燈項
- **發現 N 個死鏈**：[來源頁面] → [[不存在的目標頁面]]
- **存在 N 個未解決的知識衝突**：[頁面名稱]

### 🛠️ 下一步行動
1. 是否需要自動修復未同步索引？
2. 是否需要針對知識衝突進行重新推演？
```

## 硬約束
- **僅讀掃描**：生成報告前禁止修改任何檔案
- **手動確認**：報告後等待用戶確認再執行修復
- **修復完成後**：在 `02-wiki/log.md` 追加操作日誌
